---
title: "LLM Providers"
description: "Connect grith to Ollama, OpenAI, Anthropic, OpenRouter, or any OpenAI-compatible API"
section: "getting-started"
order: 4
tier: "community"
lastUpdated: "2026-02-24"
---

## How providers work

grith's built-in agent needs an LLM to reason about tasks. You configure one or more providers in your `config.toml` and tell grith which one to use by default. The provider handles inference; grith handles security.

<Callout type="info" title="BYOK model">
grith supports two ways to provide API keys: environment variables (via `api_key_env`) or directly in your config file (via `api_key`). Environment variables are recommended for production use; direct config is convenient for local development. Keys are never written to logs.
</Callout>

Set the default provider with:

<Terminal command="grith config set llm.default_provider ollama" />

Or override it per invocation:

<Terminal command='grith run --provider openai "summarise this codebase"' />

## Ollama (default)

Ollama runs models locally on your machine. No API keys, no network calls, no data leaving your hardware. This is the default provider.

### Setup

Install Ollama from [ollama.com](https://ollama.com) and pull a model:

<Terminal command="ollama pull llama3.1:8b" />

### Configuration

<ConfigExample
  title="Ollama provider"
  code={`[llm]
default_provider = "ollama"

[llm.ollama]
base_url = "http://localhost:11434"
model = "llama3.1:8b"`}
/>

### Recommended models

| Model | Parameters | Use case |
|---|---|---|
| `llama3.1:8b` | 8B | Fast general-purpose tasks, good for most workflows |
| `llama3.1:70b` | 70B | Complex reasoning, larger codebases |
| `codellama:34b` | 34B | Code-focused tasks |
| `deepseek-coder-v2:16b` | 16B | Code generation and analysis |

<Callout type="tip" title="GPU acceleration">
Ollama automatically uses GPU acceleration when available. For the 70B model, you need at least 48GB VRAM. The 8B model runs comfortably on 8GB VRAM or Apple Silicon with 16GB unified memory.
</Callout>

## OpenAI

Connect to OpenAI's API or any OpenAI-compatible endpoint.

### Setup

Set your API key via an environment variable:

<Terminal command="export OPENAI_API_KEY=sk-..." />

Then configure grith:

<Terminal command="grith config set llm.default_provider openai" />

### Configuration

<ConfigExample
  title="OpenAI provider"
  code={`[llm]
default_provider = "openai"

[llm.openai]
api_key_env = "OPENAI_API_KEY"    # Read key from this env var (default)
# api_key = "sk-..."              # Or set the key directly
model = "gpt-4o-mini"`}
/>

grith resolves the API key in this order: `api_key` in config (if set), then the environment variable named by `api_key_env`.

<Callout type="tip" title="Environment variables vs direct config">
Use `api_key_env` (the default) for production setups, CI, and shared machines — the key stays in your shell session or secrets manager. Use `api_key` directly in config for quick local development. Both work; choose based on your security requirements.
</Callout>

### Compatible APIs

Any API that implements the OpenAI chat completions format works with the OpenAI provider. Point `base_url` at your endpoint:

<ConfigExample
  title="OpenAI-compatible endpoint"
  code={`[llm.openai]
api_key_env = "CUSTOM_API_KEY"
model = "your-model-name"
base_url = "https://your-api.example.com/v1"`}
/>

This works with providers like Azure OpenAI, Together AI, Anyscale, Fireworks, and self-hosted vLLM or TGI instances.

## Anthropic

Connect to Anthropic's Claude API.

### Setup

<Terminal command="export ANTHROPIC_API_KEY=sk-ant-..." />

<Terminal command="grith config set llm.default_provider anthropic" />

### Configuration

<ConfigExample
  title="Anthropic provider"
  code={`[llm]
default_provider = "anthropic"

[llm.anthropic]
api_key_env = "ANTHROPIC_API_KEY"    # Read key from this env var (default)
# api_key = "sk-ant-..."             # Or set the key directly
model = "claude-sonnet-4-5-20250514"`}
/>

### Available models

| Model | Identifier | Notes |
|---|---|---|
| Claude Sonnet 4 | `claude-sonnet-4-5-20250514` | Balanced speed and capability |
| Claude Opus 4 | `claude-opus-4-20250514` | Highest capability |
| Claude Haiku 3.5 | `claude-3-5-haiku-20241022` | Fastest, lowest cost |

## OpenRouter

Connect to OpenRouter's unified API, which provides access to models from multiple providers through a single endpoint.

### Setup

<Terminal command="export OPENROUTER_API_KEY=sk-or-..." />

<Terminal command="grith config set llm.default_provider openrouter" />

### Configuration

<ConfigExample
  title="OpenRouter provider"
  code={`[llm]
default_provider = "openrouter"

[llm.openrouter]
api_key_env = "OPENROUTER_API_KEY"    # Read key from this env var (default)
# api_key = "sk-or-..."               # Or set the key directly
model = "anthropic/claude-sonnet-4-5-20250514"`}
/>

### Recommended models

| Model | Identifier | Notes |
|---|---|---|
| Claude Sonnet 4.5 | `anthropic/claude-sonnet-4-5-20250514` | Balanced speed and capability (default) |
| GPT-4o Mini | `openai/gpt-4o-mini` | Fast and cost-effective |
| Llama 3.1 70B | `meta-llama/llama-3.1-70b-instruct` | Open-weight, high capability |

## Multiple providers

You can configure several providers and switch between them. Only one is active at a time.

<ConfigExample
  title="Multiple providers"
  code={`[llm]
default_provider = "ollama"

[llm.ollama]
base_url = "http://localhost:11434"
model = "llama3.1:8b"

[llm.openai]
api_key_env = "OPENAI_API_KEY"    # Or use: api_key = "sk-..."
model = "gpt-4o-mini"

[llm.anthropic]
api_key_env = "ANTHROPIC_API_KEY"  # Or use: api_key = "sk-ant-..."
model = "claude-sonnet-4-5-20250514"

[llm.openrouter]
api_key_env = "OPENROUTER_API_KEY" # Or use: api_key = "sk-or-..."
model = "anthropic/claude-sonnet-4-5-20250514"`}
/>

Switch at runtime without editing config:

<Terminal command='grith run --provider anthropic "review this PR for security issues"' />

Or change the default:

<Terminal command="grith config set llm.default_provider anthropic" />

## Bring your own key (BYOK)

grith follows a BYOK model. Here is what that means in practice:

- **Two ways to provide keys** — set `api_key` directly in config, or use `api_key_env` to reference an environment variable. Config takes precedence if both are set.
- **Keys are never logged** — not in audit logs, not in the digest database, not in debug output
- **Keys are never transmitted to grith** — grith calls the provider API directly from your machine; no intermediate server
- **Keys are never cached** — each invocation reads the key fresh from config or the environment variable

For environment variable management, use whatever tooling you already have: shell profiles, direnv, 1Password CLI, AWS Secrets Manager, or your CI system's secret store.

<Callout type="warning" title="Config file security">
If you set `api_key` directly in `config.toml`, make sure the file has appropriate permissions (`chmod 600`). For shared machines or CI, prefer `api_key_env` so the key never touches disk.
</Callout>

## Provider selection logic

When grith starts a session, it selects a provider in this order:

1. `--provider` flag on the command line (highest priority)
2. `GRITH_LLM_PROVIDER` environment variable
3. `llm.default_provider` in per-project config (`.grith/config.toml`)
4. `llm.default_provider` in global config (`~/.config/grith/config.toml`)
5. Falls back to `ollama` if nothing is set

If the selected provider's API key environment variable is not set (for providers that require one), grith exits with a clear error message telling you which variable to set.

## Next steps

- [Quickstart](/docs/getting-started/quickstart) — Run your first secure task with a configured provider
- [Configuration](/docs/getting-started/configuration) — Full configuration reference
- [Filter Reference](/docs/reference/filters) — How grith evaluates tool calls regardless of provider
